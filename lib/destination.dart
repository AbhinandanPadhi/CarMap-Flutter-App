class Destination {
  String addressName, addressID;
  double latitude, longitude;
  // Constructor:
  Destination(
      String addressName, String addressID, double latitude, double longitude) {
    this.addressName = addressName;
    this.addressID = addressID;
    this.latitude = latitude;
    this.longitude = longitude;
  }
}
